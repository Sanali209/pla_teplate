# План заполнения шаблона (Blueprint Workflow)

На основе протокола обсуждения архитектуры `_blueprint`, процесс проектирования и планирования приложения представляет собой строгий конвейер (pipeline). Заполнение шаблона происходит сверху вниз (от сырых идей к атомарным задачам и тестам) с использованием интерактивных циклов ревью и обеспечения реверс-пути (Traceability).

Ниже представлен пошаговый план заполнения шаблона для нового проекта:

## Фаза 0: Сбор и загрузка (Inbound)
**Цель:** Собрать исходную информацию без попыток её сразу структурировать.
1. Сохранить все текстовые заметки, транскрипты интервью и брифы от заказчика в `_blueprint/inbound/Briefings/`.
2. Сохранить визуальные материалы (Miro, концепты, черновики) в `_blueprint/inbound/MindMaps/`.
3. Сохранить ссылки на внешнюю документацию и референсы API в `_blueprint/inbound/Knowledge_Raw/`.
4. **Запуск агента (P0_Ingestion):** Агент анализирует сырье и готовит выжимку ключевых бизнес-намерений для следующего шага.

## Фаза 1: Стратегическое целеполагание (Inception)
**Цель:** Сформировать бизнес-фундамент и границы проекта.
1. **Запуск агента (P1_Inception).**
2. **Создание Целей (Goals):** Агент генерирует документы вида `GL-001.md` в папке `docs/Vision_and_Goals/` (на основе `templates/Goal_Tpl.md`).
3. **Декомпозиция Целей (P1_5_Goal_Decomposition):** Если базовая цель слишком абстрактная или крупная, агент разбивает её на под-цели (создавая иерархию через `parent_goal`).
4. **Составление списка фич:** Формирование высокоуровневых функциональных блоков (`docs/Feature_Map.md`) с привязкой к конкретным (под)целям.
5. **Формирование Roadmap:** Заполнение `_blueprint/execution/roadmap.md` основными вехами.
6. **Ревью:** Пользователь утверждает цели. (Включается протокол `S1_Wait_For_Approval`).

## Фаза 2: Снятие неопределенностей (Research & R&D)
**Цель:** Исключить технологические риски до начала проектирования логики.
1. Идентификация сложных/новых фич, помеченных тэгом `research_required: true`.
2. **Запуск агента (P2_Research).**
3. Формирование гипотез и вопросов для исследования (Research Spikes).
4. Проведение экспериментов (POC) и запись вердиктов в `_blueprint/dev_docs/brain/R_D_Archive/RS-xxx.md`.
5. Если вердикт "Успешно" — переход к проектированию логики.

## Фаза 3: Проектирование поведения и логики (Analysis)
**Цель:** Детализировать фичи до конкретных сценариев использования и пользовательских путей.
1. **Запуск агента (P3_Analysis).**
2. **Определение Use Cases:** Для каждой фичи формируются `UC-xxx.md` в `_blueprint/dev_docs/logic/`.
3. **Создание User Flows:** Описание переходов между экранами и состояниями.
4. В метаданных каждого `UC-xxx.md` (согласно `Metadata_Schema.md`) агент прописывает ссылку на родительские сущности (Origin, Parent Goal).

## Сквозная Фаза: Визуализация архитектуры (P3_5_UML_Generator)
**Цель:** Визуализировать архитектуру и найти логические дыры. UML генерируется не один раз, а на разных этапах конвейера в зависимости от типа диаграммы:
*   **После P1 (Inception):** `Use Case Diagram` — показать акторов и границы системы.
*   **После P3 (Analysis):** `Activity Diagram` (ветвления бизнес-процесса) и `Sequence Diagram` (поток сообщений между компонентами).
*   **Перед P4 (Dev Sync):** `Class/ER Diagram` (структура данных) и `State Machine` (состояния ключевых сущностей).

**Процесс генерации:**
1. Агент (Роль: UML Modeler) применяет `P3_5_UML_Generator` к утвержденному Use Case или цели.
2. Авто-валидация синтаксиса через `validate_uml`.
3. Самокритика агента (`run_self_critique`).
4. Сохранение черновика в `_blueprint/dev_docs/architecture/UML_Models/Drafts/`.
5. После утверждения пользователем (через GUI) диаграмма переносится в `Approved/`.

## Фаза 5: Инженерная синхронизация и Фазинг (Dev Sync)
**Цель:** Подготовить проект к написанию кода и обеспечить отказоустойчивость.
1. Когда все Use Cases и диаграммы перешли в статус `Approved`, запускается **P4_Dev_Sync**.
2. **Проектирование данных:** Создание схем баз данных и спецификаций API в `_blueprint/dev_docs/architecture/Data_Schemas/`.
3. **Фазинг и отказоустойчивость:** Генерация векторов атак и граничных условий (`Fuzzing_Vectors.md`, анализ точек отказа).
4. **Генерация задач (Backlog):** Нарезка сценариев на атомарные задачи (`TSK-xxx.md`) в `_blueprint/execution/backlog/`.
   * *Важно:* В атрибутах задач прописан полный реверс-путь до бизнес-цели (обеспечивая полную прослеживаемость).

## Фаза 6: Планирование, Выполнение Спринта и Накопление опыта (Sprint Execution)
**Цель:** Разбить беклог на выполнимые итерации, реализовать код и сразу же консолидировать полученные знания.
1. **Планирование (P5_Sprint_Planning):** Роль Scrum Master-а анализирует `_blueprint/execution/backlog/`, отбирает `TSK-xxx` готовые к взятию и формирует спринт-доску `_blueprint/execution/sprint_current.md`. Задачи переводятся в `IN_PROGRESS`.
2. **Выполнение (E1_Sprint_Execution):** Автономный агент берёт задачу в работу, пишет код и покрывает юнит-тестами (по данным фазинг-векторов).
3. **Логирование сессий:** Каждое действие или баг агент фиксирует в ежедневный лог в `_blueprint/execution/session_logs/`.
4. **Сбор новых знаний (Continuous Harvesting):** Свежие концепции и паттерны фиксируются агентом на лету в `_blueprint/inbound/Knowledge_Raw/new_learnings.md`.
5. По завершении `TSK-xxx` переводится в `DONE`.
6. **Консолидация знаний (Brain Update):** Сразу после завершения задачи агент обновляет `Design_Patterns.md`, `Anti_Patterns.md` и `Terminology.md` в папке `_blueprint/dev_docs/brain/` на основе полученного опыта.

---

### Правила при заполнении:
1. **Обязательное заполнение метаданных:** Каждый созданный файл должен соответствовать стандарту `Metadata_Schema.md` для корректной работы реверс-пути. Никаких файлов-сирот.
2. **Никакой работы в обход статусов:** Нельзя генерировать задачи, пока соответствующий `Use Case` или `UML` не получит статус `Approved`.
3. **Изоляция исполнения:** Вся работа агентов происходит внутри скрытой директории `_blueprint/`, а публичные документы регулярно синхронизируются в пайплайне в корень проекта (папка `docs/`).

---

## 🚦 Сценарии Использования Системы (Usage Scenarios)

Система Blueprint гибка и адаптируется под разные объемы работы. Вот как применять протоколы в зависимости от вашей задачи:

### Сценарий 1: Запуск нового проекта с нуля (Greenfield)
1. **Сброс:** Вы кладете ТЗ в `inbound/Briefings/`.
2. **Итерация:** `P0` (Анализ ТЗ) ➔ `P1` (Создание Целей GL-xxx) ➔ `P3.5` (Use Case диаграмма).
3. **Генерация фич:** `P1.5` (если Цели крупные) ➔ Создание `FT-xxx`.
4. **Детализация:** `P3` (Создание `UC-xxx`) ➔ `P3.5` (Sequence/Activity диаграммы).
5. **Разработка:** `P4` (Задачи + ER диаграммы) ➔ `P5` (Спринт) ➔ `E1` (Код).
*Итог: Проект проходит весь конвейер слева направо.*

### Сценарий 2: Добавление изолированной новой Фичи (Feature Addition)
У вас уже есть работающий проект с Целями, и вы хотите добавить "Систему уведомлений".
1. **Создание:** Вы просите агента запустить `P1_Inception`, указав родительскую цель (например, `GL-002: Улучшение Retention`).
2. Агент создает ОДНУ фичу `FT-004: Push-уведомления`.
3. **Прогонка фичи:** Запуск `P3` только для этой фичи ➔ `P4` ➔ `E1`.
*Мы не трогаем весь остальной Blueprint, пайплайн запускается точечно для `FT-004`.*

### Сценарий 3: Рефакторинг или Изменение Требований
Бизнес решил, что "Система оплаты" (уже реализованная) должна поддерживать крипту.
1. **Отзыв артефакта:** Вы переводите статус `FT-002: Оплата` и связанной `UC-005` из `APPROVED` обратно в `REVIEW` или `NEEDS_FIX`.
2. **Переработка логики:** Агент применяет протокол `R3_Fix_and_Refactor` для обновления `UC-005` под крипту. Запускается `P3.5` для обновления диаграмм.
3. **Нарезка новых задач:** Запускается инкрементальный `P4_Dev_Sync`. Протокол использует `search_artifacts`. Он видит старые выполненные задачи TSK-xxx, оставляет их в покое, и генерирует новые задачи (например, `TSK-010: Интеграция Crypto API`).
*Итог: Архитектура эволюционирует без дублирования и хаоса.*

### Сценарий 4: Разведка новой технологии (Research Spike)
Вы хотите внедрить AI, но не знаете какую модель выбрать.
1. Агент создает `FT-005: AI Помощник` с флагом `research_required: true`.
2. Запускается исключительно `P2_Research`.
3. Агент пишет прототипы в `dev_docs/brain/R_D_Archive/RS-001.md`.
4. Когда понятно как делать, флаг снимается, и фича идет дальше в `P3`.

### Сценарий 5: Управление Техническим Долгом (Tech Debt)
В процессе написания кода агент или разработчик пошел на компромисс (хардкод, костыль).
1. **Регистрация долга:** Прямо во время `E1_Sprint_Execution` агент применяет тул `create_artifact` для создания новой задачи `TSK-xxx` с меткой `[Tech Debt]` в заголовке.
2. **Привязка:** В качестве `parent_uc` указывается тот же Use Case, над которым сейчас идет работа (чтобы долг не висел в воздухе).
3. **Бэклог:** Эта задача падает в папку `backlog/` в статусе `DRAFT`.
4. В следующем спринте Scrum Master (`P5`) может включить эти задачи в работу для рефакторинга.

### Сценарий 6: Обработка Баг-Репортов (Bug Triage)
Пользователь или QA прислал отчет об ошибке с продакшена.
1. **Загрузка:** Отчет кладется в папку `inbound/Issues_and_Bugs/`.
2. **Анализ (P0.5):** Запускается `P0_5_Bug_Triage`. Агент читает баг, ищет виновный Use Case через `search_artifacts` и анализирует корень проблемы.
3. **Решение (Код):** Если это ошибка реализации, агент сразу создает задачу `[BUG] TSK-xxx` в бэклоге.
4. **Решение (Архитектура):** Если сценарий вообще не был учтен, агент отправляет `UC-xxx` на `REVIEW` и предлагает добавить новое требование.
